version: '3.3'

services:

  db:
    container_name: shop_postgres
    image: postgres:14.1-alpine
    restart: always
    env_file: .env
    ports:
      - 5432:5432

  adminer:
    container_name: shop_adminer
    image: adminer
    restart: always
    ports:
      - 8080:8080
